# SPDX-License-Identifier: MIT
#
# This file is rendered by run_public_input.sh. The build config includes all
# OQS-owned public API dispatch files; runner configs narrow the same template
# to the API family exercised by each test binary.

quiet: false
strict: false
compilationDatabasePath: "@BUILD_DIR@/compile_commands.json"
gitProjectRoot: "@PROJECT_ROOT@"
includePaths:
@INCLUDE_PATHS@
excludePaths:
  - ".*/tests/.*"
  - ".*/src/common/.*"
